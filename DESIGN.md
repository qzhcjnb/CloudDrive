# CloudDrive — Design Rules

面向个人资料管理者的静态文件浏览产品。设计目标只有一个：让**找文件、看来源、下载文件**比任何装饰都更突出。

## 定位

- 气质：安静、精确、有收藏感的**文件工作台**，不是 SaaS 仪表盘。
- 视觉变化度 4/10，动态强度 3/10，信息密度 7/10。
- 文件是页面记忆点；不靠大卡片、渐变、玻璃质感制造层级。

## 设计系统（Token）

`css/style.css` 顶部的 CSS 变量是**唯一运行时 Token 来源**。组件只允许引用变量，不允许写死颜色/圆角/间距/时长。

| 类别 | 亮色 | 深色 |
|---|---|---|
| 页面底色 `--bg` | `#F5F7F9` | `#12171B` |
| 表面 `--surface` | `#FFFFFF` | `#181E24` |
| 下沉表面 `--sunken` | `#EEF1F5` | `#1F262D` |
| 主文字 `--text` | `#161D24` | `#E6ECF1` |
| 次级文字 `--muted` | `#5F6B76` | `#93A0AB` |
| 分隔线 `--line` | `#D9E0E7` | `#2A333B` |
| 强调色 `--accent` | `#1E5A96` | `#7FB0DC` |
| 强调浅底 `--accent-soft` | `#E7EEF6` | `#1B2A39` |
| 强调色上的文字 `--on-accent` | `#FFFFFF` | `#0B1520` |
| 警示 `--warn` | `#8A5A17` | `#D9A75A` |

- 间距阶：`4 / 8 / 12 / 16 / 24 / 32 / 48`
- 圆角：**整体偏方**。控件 `2px`、芯片/标签 `2px`、面板 `3px`、弹层 `4px`。不使用胶囊（`999px`）和圆形（`50%`）作为控件外形。
- 阴影：**仅**详情抽屉、菜单、弹层、Toast 使用，且压缩到短距离小锥形（面板 `0 2px 6px`、弹层 `0 10px 28px`）；文件列表永远平面
- 动效：`150–220ms`，只动 `transform` 与 `opacity`/`background`；`prefers-reduced-motion` 下全部关闭
- 层级：顶栏 → 内容 → 浮层遮罩 → 弹层 → Toast

字体：`--font-ui` 系统中文混排栈（不用 Inter）；大小、日期、扩展名等数据用 `--font-mono` 等宽栈 + `tabular-nums`。

## 结构

桌面：

```text
产品栏：CloudDrive / 搜索(可清除) / 刷新 / 主题
当前位置：首页 / 游戏 / CS2
工具栏：筛选 · 排序 · 视图 · 当前结果
列表头：类型与名称 | 标签 | 来源或类型 | 大小 | 更新时间
```

移动端：

```text
品牌与控制项
完整宽度搜索
压缩面包屑
横向筛选与排序栏
两行文件行（名称优先，元数据第二行）
底部详情 Sheet
```

## 文件脊线

每行左侧「类型图标 → 细分隔线 → 文件名」构成统一索引带：图标列有右分隔线且铺满整行高度，相邻行连成一条竖向脊线，让列表本身成为识别特征。列表头、行、骨架共用 `--row-grid` 五列栅格，保证列对齐。

## 图标

全部使用单色线性 SVG（`stroke="currentColor"`），**不再用 Emoji 作为图标系统**。图标按扩展名映射到固定的语义集合（文件夹/压缩/图片/视频/音频/代码/文档/程序/通用文件）。

## 交互与可达性

- 文件行是 `<li>` + 「拉伸命中层」：文件名是一个真 `<button>`，其 `::after` 铺满整行作为点击区；标签按钮排在它之后且 `position: relative`，所以点标签是筛选、点其他位置是打开。这样既有整行可点，又能行内嵌套交互控件，且**不使用 `<button>` 嵌套 `<button>`**。
- 列表头的「类型与名称 / 大小 / 更新时间」是真按钮：点击排序，重复点击翻转方向，`aria-sort` 随状态更新。
- 加载中显示与最终列表同构的**骨架行**，而不是提前显示空状态；错误状态提供「重试」。
- 搜索框有独立清除按钮；筛选按钮带 `aria-expanded` 展开态；平台筛选器只在 `release` 目录出现。
- 详情抽屉：`Escape` 关闭、打开时焦点进入、关闭时焦点回到触发元素、`Tab` 焦点循环。
- 状态文案精简（`当前结果 N 项`，分块时追加 `已显示 M 项`）。
- 动效只用于按钮按压、抽屉/弹层、列表分批与筛选状态变化，全部受 `prefers-reduced-motion` 控制。

## 性能契约

- 索引缓存走 **IndexedDB**（`js/cache.js`），不用 localStorage：大型仓库的 Release 索引会超出 5 MB 配额且 localStorage 写入失败是静默的。缓存键带 schema 版本。
- 长列表**分块渲染**：首批 150 行，`IntersectionObserver` 哨兵滚入时继续追加；Releases 额外**折叠为每版本一行**。这是为了让 4000+ Asset 的仓库仍能秒开。
- 仓库项与 Release 项都来自单次 API 响应后在内存中搜索/筛选/排序，不对每个文件额外请求。

## 明确移除 / 保留

**移除**：蓝紫色默认感、玻璃拟态顶栏、层层圆角卡片、胶囊形芯片与圆形按钮、大而柔的投影、Emoji 图标、无意义统计文案、等权重的筛选卡/文件卡/信息卡。

**保留**：CloudDrive 品牌名、目录与 Release 信息架构、全部功能（搜索/筛选/排序/详情/下载/预览）、亮/暗/系统主题、Hash 路由与静态部署兼容性。
